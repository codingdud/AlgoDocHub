#include <iostream>
#include <string>
using namespace std;

int main() {
    string str1, str2;
    int index1, index2;
    getline(cin, str1);
    getline(cin, str2);

    // Output the swapped strings
    cout << str2 << endl;
    cout << str1 << endl;

    return 0;
}
